{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window during Phase 0 scaffold.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
