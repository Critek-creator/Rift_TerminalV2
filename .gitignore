# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Rust / Tauri (added pre-emptively for upcoming impl)
target/
**/*.rs.bk
Cargo.lock.bak

# Node / web frontend
node_modules/
dist/
.cache/
.parcel-cache/
*.log
.env
.env.local

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# Claude Code project-local state
.claude/

# Aegis Verifier session state (ephemeral — verify.toml + lessons/ ARE committed)
.aegis/session/

# ---------------------------------------------------------------
# Aegis private translator (Phase 7 — feature-gated optional dep)
# ---------------------------------------------------------------
# The rift-aegis crate is private. Never committed to the public
# repo. Mechanism: optional path dep + feature flag in src-tauri
# (see src-tauri/Cargo.toml [features] aegis). Public CI builds
# default features → rift-aegis path never resolved → green.
/crates/rift-aegis/
